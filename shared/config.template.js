/**
 * ArcGIS API configuration template
 * 
 * Copy this file to config.js and replace with your actual API key.
 * 
 * For development: Use a key with no referrer restrictions
 * For production: GitHub Actions will inject the production key automatically
 * 
 * Setup instructions:
 * 1. Copy this file: cp shared/config.template.js shared/config.js
 * 2. Edit shared/config.js and replace YOUR_API_KEY_HERE with your actual key
 * 3. Never commit the config.js file (it's gitignored for security)
 */
window.arcgisConfig = {
    apiKey: "YOUR_API_KEY_HERE"
}; 